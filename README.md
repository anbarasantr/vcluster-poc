# vcluster-poc
- Setup a local host K8s cluster by installing Kind (Kubernetes in docker) in MacOS
- Install Vcluster and other softwares like helm, kubectl
- Create 3 tenant clusters in the Host cluster
- Deploy wordpress website contains stateless and stateful components into the 3 tenant clusters
